export interface RecetaIngrediente {
  id: RecetaIngrediente;
  cantidad: number;
  unidadMedida: string;
  fechaCreacion?: string;
  usuarioCreacion?: string;
}

// Debes definir la interfaz RecetaIngredienteId según la estructura de la clave compuesta en Java.
